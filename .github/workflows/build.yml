name: build

on:
  push:
    branches: [master]
    paths:
    - wordlists/**
    - dirtytalk.py
    - pyproject.toml
    - .github/workflows/build.yml
  pull_request:
    branches: [master]
  schedule:
  # Yearly: January 1st at 10:00 UTC.
  - cron: 0 10 1 1 *
  workflow_dispatch:
    inputs:
      refresh:
        description: Run "dirtytalk fetch" to refresh wordlists from upstream sources.
        required: false
        default: false
        type: boolean

permissions:
  contents: write

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4.2.2
    - uses: astral-sh/setup-uv@v6.3.1
    - run: uv tool run pre-commit run --all-files --show-diff-on-failure

  build:
    needs: lint
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4.2.2
    - uses: astral-sh/setup-uv@v6.3.1
    - run: uv sync
    - if: ${{ inputs.refresh || github.event_name == 'schedule' }}
      run: uv run dirtytalk fetch
    - run: uv run dirtytalk build
    - name: Commit build artifacts back to master
      if: |
        github.ref == 'refs/heads/master' &&
        (github.event_name == 'push' || github.event_name == 'workflow_dispatch' || github.event_name == 'schedule')
      run: |
        git config user.name  "github-actions[bot]"
        git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
        git add wordlists programming.words
        if git diff --cached --quiet; then
          echo "No changes to commit."
        else
          git commit -m "Rebuild wordlist [skip ci]"
          git push
        fi
